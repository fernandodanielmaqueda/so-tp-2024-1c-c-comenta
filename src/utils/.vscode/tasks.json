{
  // See https://go.microsoft.com/fwlink/?LinkId=733558
  // for the documentation about the tasks.json format
  "version": "2.0.0",
  "tasks": [
    {
      "label": "make src/utils/bin/libutils.a",
      "command": "make src/${workspaceFolderBasename}/bin/lib${workspaceFolderBasename}.a",
      "type": "shell",
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": ["$gcc"],
	  "options": {
        "cwd": "${workspaceFolder}/../../"
      }
    }
  ]
}
